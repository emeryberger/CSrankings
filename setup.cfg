[flake8]
ignore = E501,E302,W503,W504

[tool:pytest]
markers =
    postmake: invariant that only holds after `make` has normalized the tree; deselected in the gating test run and re-checked after `make` in the build job
